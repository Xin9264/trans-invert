请评估用户的英文回译答案，并严格返回 JSON 格式的结果（不要添加任何其他文字或解释）。

⸻

输入格式

原文：
“{{ original_text }}”

中文翻译（参考）：
“{{ translation }}”

用户输入：
“{{ user_input }}”

⸻

输出格式

请严格按照以下 JSON 格式返回结果：

{
  "score": 分数(0-100的整数),
  "corrections": [
    {
      "original": "用户写错或遗漏的部分",
      "suggestion": "建议的正确写法或补充内容",
      "reason": "错误或遗漏的原因说明（语法/用词/搭配/语义/完整性）"
    }
  ],
  "overall_feedback": "整体评价和改进建议（必须涵盖语法、词汇、结构、完整性和表达自然度，不可泛泛而谈）",
  "is_acceptable": true/false
}


⸻

评分标准
	•	90-100 优秀：语法正确，表达自然，意思准确，复杂度与原文接近，覆盖完整。
	•	80-89 良好：基本正确，可能有轻微语法问题或略简化，但整体语义完整。
	•	70-79 可以：语义大体正确，但存在明显语法错误，或遗漏部分关键信息，或过度简化。
	•	60-69 一般：语法问题较多，或缺少重要信息/整段内容，影响完整性。
	•	0-59 需要改进：语法和语义错误严重，缺失大量内容，偏离原文。

⸻

重要原则
	1.	完整性检查：必须检查用户是否覆盖了原文所有主要内容（包括段落、事件、地点、因果关系、数字等）。
	•	若缺少整段 → 最高 70 分。
	•	若缺少部分关键信息 → 最高 80 分。
	2.	语法与词汇检查：必须逐一检查时态、冠词、介词、单复数、搭配、句式。哪怕是轻微错误也必须指出。
	3.	复杂度要求：如果用户回避原文复杂语法（如从句）而改成过于简单的句子，应扣分。
	4.	corrections 不得为空：
	•	若没有明显错误，必须至少给出 1 条「表达优化建议」。
	•	若有错误或遗漏，必须逐条列出。
	5.	表达差异的容忍度：允许合理改写，但如果造成语义弱化、歧义或不完整，必须扣分。
	6.	overall_feedback：必须同时指出优点（如表达流畅度、部分语法准确性）和不足（如遗漏点、错误、简化），并给出具体改进方向。
	7.	is_acceptable 判断：
	•	语法基本正确 + 内容完整 → true。
	•	如果有严重语法错误或缺失关键信息导致理解不完整 → false。
	8.	严格输出 JSON：除 JSON 外不得输出任何解释。

⸻