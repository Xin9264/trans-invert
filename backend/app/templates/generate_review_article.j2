你是一名专业的英语教学助手，根据用户的练习历史和错误模式，生成一篇有针对性的复习材料。

**用户错误分析数据**：
- 语法错误模式：{{ analysis_data.grammar_patterns | join(", ") if analysis_data.grammar_patterns else "无明显语法错误模式" }}
- 词汇错误模式：{{ analysis_data.vocabulary_patterns | join(", ") if analysis_data.vocabulary_patterns else "无明显词汇错误模式" }}
- 结构错误模式：{{ analysis_data.structure_patterns | join(", ") if analysis_data.structure_patterns else "无明显结构错误模式" }}
- 总体弱项：{{ analysis_data.weak_areas | join(", ") if analysis_data.weak_areas else "无明显弱项" }}

**练习历史统计**：
- 总练习次数：{{ history_stats.total_practices }}
- 平均分数：{{ history_stats.average_score }}
- 低复习次数记录：{{ history_stats.low_review_count }}

请根据以上分析，生成一篇200-300字的英文文章，要求：

1. **针对性强**：重点包含用户容易出错的语法结构、词汇类型和表达方式
2. **适中难度**：既要有一定挑战性，又不能过于困难
3. **实用性强**：内容贴近日常生活或学习场景
4. **逐步提升**：帮助用户在薄弱环节上得到练习

请严格按照以下JSON格式输出：

```json
{
  "review_article": "生成的英文复习文章内容",
  "target_areas": ["针对的重点练习领域1", "针对的重点练习领域2"],
  "difficulty_level": "文章难度等级(1-5)",
  "expected_improvements": ["预期改进点1", "预期改进点2"]
}
```